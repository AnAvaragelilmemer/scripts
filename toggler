local library = loadstring(game.HttpGet(game, "https://raw.githubusercontent.com/bloodball/-back-ups-for-libs/main/0x"))()


local w1 = library:Window("Homebrew toggler")


w1:Button(
    "Enable",
    function()
for i, v in pairs(game.CoreGui.Discord:GetDesendants()) do
        v.Visible = true   
        end
    end
) -- Text, Callback



w1:Button(
    "Disable",
    function()
   for i, v in pairs(game.CoreGui.Discord:GetDesendants()) do
        v.Visible = false   
   end
    end
) -- Text, Callback
